{
  "name": "sealdice-ocr-backend",
  "version": "1.0.0",
  "description": "Tesseract OCR backend for the SealDice image recognition plugin",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup-langs": "node setup-langs.js",
    "check": "node --check server.js && node --check ocr.js && node --check downloader.js && node --check setup-langs.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1",
    "tesseract.js": "^7.0.0"
  }
}
